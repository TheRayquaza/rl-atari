defaults:
  - agent: qlearning
  - env: default
  - training: default
  - evaluation: default
  - _self_

experiment:
  name: "rl_experiment"
  seed: 42
  output_dir: "outputs"

hydra:
  run:
    dir: ${experiment.output_dir}/${experiment.name}/${now:%Y-%m-%d_%H-%M-%S}
